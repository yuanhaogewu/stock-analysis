# Antigravity AI IDE – 6A Planning 工作流增强版规范 v2

本规范用于指导 **Antigravity AI IDE 在 Planning 模式下**进行复杂软件项目的系统化规划与执行。

本版本不是传统人类瀑布式项目管理，而是为 **AI 长时间协作、上下文有限、可中断执行** 场景优化的增强工作流。

---

# 一、核心设计理念（适配 Antigravity Planning 模式）

本流程的设计目标从“人类项目管理逻辑”升级为“AI 可持续执行逻辑”。

核心原则如下：

- 允许在不完全确定中前进，而不是等待完美信息
- 文档必须可压缩、可恢复，而不是无限膨胀
- 流程允许回滚与跳跃，而不是严格单向线性
- 决策分级管理，而不是所有问题都人工确认
- 面向持续演进，而不是一次性交付终止

---

# 二、整体流程结构（循环式 6A 架构）

Planning 流程采用可回退循环结构，而非线性瀑布流。

```
Align ⇄ Architect ⇄ Atomize  
        ↓  
     Automate  
        ↓  
      Assess  
        ↺（如发现偏差，可回滚）
```

## 回滚规则

当任一阶段发现以下情况时，允许直接回到前置阶段：

- 需求边界发生变化 → 回到 Align  
- 架构不再适配实现 → 回到 Architect  
- 任务拆分复杂度失控 → 回到 Atomize  

禁止“带着已知错误继续推进”。

---

# 三、三层记忆文档系统（解决 AI 上下文衰减）

为适应 AI 上下文窗口限制，文档分为三层。

## 1. 核心约束层

**文件：PROJECT_RULES.md**  

内容包括：

- 技术栈约束  
- 架构原则  
- 安全规范  
- 禁用技术  

特性：稳定、极短、长期有效

## 2. 当前任务层

**文件：CURRENT_TASK.md**  

内容包括：

- 当前目标  
- 明确边界  
- 关键设计决策  
- 当前阶段进度  

特性：动态更新，是 Planning 模式的主上下文来源

## 3. 历史归档层

**目录：docs/任务名/***  

保存完整阶段文档，仅用于回溯。

## 强制规则

每个阶段结束必须生成：

**任务名_SUMMARY.md**  
要求：不超过 300 行，作为持续可读的压缩记忆版本。

---

# 四、Align 阶段（允许带假设前进）

## 阶段目标

将模糊需求转为可执行规范，但不追求一次性消除全部不确定性。

## 决策置信度机制

| 决策类型 | 处理方式 |
|----------|----------|
| 高风险架构决策 | 必须询问确认 |
| 中等影响设计 | 给出默认方案，允许后续调整 |
| 低影响细节 | AI 自主决策并记录假设 |

## 假设驱动规则

当信息不足时：

- 不中断主流程  
- 给出默认实现方案  
- 标记为：**Assumption-Based Decision**  

---

# 五、Architect 阶段（可回滚架构设计）

## 阶段目标

定义系统结构、模块边界、核心组件关系。

## 新增规则

- 若后续阶段发现设计不合理，可回到本阶段修订  
- 设计文档必须包含“可演化说明”，标记未来可能变化的部分  
- 避免为未知远期需求过度设计  

---

# 六、Atomize 阶段（引入 AI 复杂度分级）

任务拆分不仅按工程逻辑，还要适配 AI 成功率。

## AI 任务复杂度等级

| 等级 | 描述 | 处理规则 |
|------|------|-----------|
| L1 | 小函数 / 简单逻辑 | 直接实现 |
| L2 | 单文件模块 | 实现 + 单元测试 |
| L3 | 跨模块功能 | 必须继续拆分 |
| L4 | 架构级改动 | 回到 Architect |

## 强制字段

每个 TASK 文档必须包含：

**任务复杂度等级：L1 / L2 / L3 / L4**

---

# 七、Approve 阶段（从完美审查转为可控审查）

审批目标从“无遗漏覆盖”转为“风险可控”。

允许存在：

- 可延后优化项  
- 不影响主流程的技术债  

但必须明确记录。

---

# 八、Automate 阶段（AI 增量执行模型）

执行阶段采用增量模型，而非一次性完成全部实现。

## 增量实现规则

- 每完成约 30% 功能必须运行测试  
- 禁止长时间不验证的连续编码  

## 差异感知规则

每次修改代码前必须说明：

- 本次改动影响范围  
- 涉及模块  
- 潜在风险  
- 是否影响已有接口  

## 文档同步规则

每个子任务完成后，必须更新 **CURRENT_TASK.md** 摘要。

---

# 九、Assess 阶段（加入演化与技术债视角）

评估不仅检查是否完成，还要关注未来可维护性。

## 必须生成文档

**ARCHITECTURE_DEBT.md**

内容包括：

- 为赶进度做的妥协设计  
- 当前可优化结构  
- 建议未来重构点  

---

# 十、FailSafe 中断恢复机制（AI 长任务生存关键）

任何阶段中断必须生成 **FAILSAFE.md**

内容格式：

```
当前阶段：
当前任务：
已完成内容：
阻塞原因：
下一步需要的信息：
```

用于 AI 在会话中断或上下文丢失后快速恢复执行。

---

# 十一、Planning 模式行为准则（Antigravity 专用）

在 Planning 模式下，AI 必须遵守以下行为规则：

1. 始终优先读取 **CURRENT_TASK.md**  
2. 决策前判断风险等级  
3. 遇到 L4 任务自动回退到 Architect 阶段  
4. 不因细节不确定而停滞主流程  
5. 每阶段结束输出 SUMMARY 文档压缩记忆  
6. 发现结构性问题优先回滚，而不是打补丁前进

---

# 十二、语言与技术默认约定（最高优先级规则）

本节规则为全局默认规则，如与其他规则冲突，以本节为准。

## 一、语言与输出约定

- 所有回复、说明、注释、文档 **必须使用简体中文**
- Planning 模式生成的所有 Implementation 文件（含 implementation 目录、Implementation.md、*_IMPLEMENTATION.md、以及任何被标记为 implementation 的输出文件）中，除代码语法本身外，所有自然语言内容必须为简体中文（包括：步骤说明、设计解释、列表项、标题、注释段落、变更说明、注意事项等）。
- 如果模板/脚手架默认英文，必须在生成时立即将文件内的自然语言文本改写为简体中文后再提交输出；不得保留英文版占位说明（如 TODO/NOTE/Steps/Overview 等英文段落）。
- 代码中的标识符保持英文，不使用拼音
- 错误信息、日志内容允许为英文

## 二、技术默认约定

在未特别说明的情况下，技术选型默认如下：

- 前端：**React + TypeScript**
- 后端：**Python（优先 FastAPI）**

若需求未指定技术栈，AI 必须自动采用以上默认技术方案。

## 三、文档同步规则（强化）

- 代码发生任何功能性或结构性更新时，必须同步更新相关文档
- 包括但不限于：CURRENT_TASK.md、DESIGN 文档、API 接口说明
- 禁止出现“代码已变更但文档未更新”的情况

---

# 十三、Git 协作与版本控制规范

为保证 AI 与人类协作时的代码可追溯性与结构清晰度，必须遵循以下 Git 规范。

## 1. 分支命名规范

分支名称必须语义清晰，采用以下格式：

- 新功能：feature/功能名
- 修复问题：fix/问题简述
- 重构优化：refactor/模块名
- 文档更新：docs/内容说明
- 实验性尝试：experiment/主题

禁止使用无意义命名（如 test、aaa、temp）。

## 2. 提交信息规范

提交信息必须说明“做了什么 + 为什么”，推荐格式：

类型: 简要说明

示例：
feat: 新增用户登录接口并添加参数校验
fix: 修复订单状态更新不同步问题
refactor: 重构支付模块解耦业务逻辑

常用类型：
- feat 新功能
- fix 修复问题
- refactor 重构
- docs 文档
- chore 构建或工具变更

## 3. AI 提交行为约定

AI 在自动化执行阶段生成代码变更时：

- 必须为每个子任务生成独立提交
- 提交前必须通过测试
- 提交信息必须引用对应 TASK 名称

---

# 十四、分支流程与合并策略

## 默认分支结构

- main：稳定可运行版本
- dev：当前开发主线
- feature/*：功能开发分支

## 合并规则

- 所有功能必须从 dev 分支拉出 feature 分支开发
- 完成功能后合并回 dev
- main 只接受稳定版本合并
- 禁止直接向 main 提交代码

---

# 十五、接口与模块命名规范

为保证项目长期可维护性，接口与模块命名需保持一致性与可读性。

## 接口命名规范（REST 风格）

- 使用名词而非动词
- 使用复数形式表示集合

示例：
GET /users        获取用户列表  
GET /users/{id}   获取单个用户  
POST /orders      创建订单  
PUT /orders/{id}  更新订单  
DELETE /orders/{id} 删除订单

## 前端组件命名规范

- 使用 PascalCase
- 文件名与组件名一致

示例：
UserCard.tsx  
OrderList.tsx  

## 后端模块命名规范

- 使用小写 + 下划线
- 模块名应体现业务语义

示例：
user_service.py  
order_controller.py  

---

# 十六、代码风格与结构规范

## 通用原则

- 函数应短小、单一职责
- 避免超长文件（建议 < 500 行）
- 禁止重复逻辑，应抽取复用模块

## 注释规范

- 注释说明“为什么”，而不是“做了什么”
- 关键逻辑必须有注释说明业务意图

---

# 十七、测试与质量保障规范

## 测试基本要求

- 新功能必须包含单元测试
- 修复 Bug 必须包含回归测试
- 核心业务逻辑测试覆盖率应优先保障

## AI 执行测试规则

- 每完成一个子任务必须运行测试
- 测试失败禁止继续实现新功能

---

# 十八、长期演进与技术债管理

AI 在 Assess 阶段必须主动识别并记录：

- 临时性实现方案
- 可复用但尚未抽象的逻辑
- 潜在性能风险点
- 未来可拆分模块

统一记录在：ARCHITECTURE_DEBT.md

技术债不是错误，而是需要被持续管理的工程现实。
